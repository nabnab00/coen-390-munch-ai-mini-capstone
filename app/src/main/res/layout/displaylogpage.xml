<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/munch_anti_flash_white"
    tools:context=".frontend.DisplayLogActivity">

    <!-- Back Button -->
    <ImageButton
        android:id="@+id/backDisplayButton"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:padding="8dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_arrow_back"
        android:contentDescription="@string/back"
        app:tint="@color/munch_forest"
        android:layout_marginStart="8dp"
        android:layout_marginTop="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Delete Button -->
    <ImageButton
        android:id="@+id/deleteMealButton"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:padding="8dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_delete"
        android:contentDescription="Delete meal"
        app:tint="@color/munch_forest"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <!-- Title -->
    <TextView
        android:id="@+id/log_detail_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Log Details"
        android:textColor="@color/munch_forest"
        android:textSize="22sp"
        android:fontFamily="@font/outfit_semibold"
        android:letterSpacing="0.02"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@id/backDisplayButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Image Preview -->
    <ImageView
        android:id="@+id/log_image_preview"
        android:layout_width="0dp"
        android:layout_height="220dp"
        android:layout_marginTop="12dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:scaleType="centerCrop"
        android:background="@drawable/bg_image_placeholder"
        android:visibility="gone"
        tools:visibility="visible"
        android:contentDescription="Meal Image"
        app:layout_constraintTop_toBottomOf="@id/log_detail_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Scrollable Content -->
    <ScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@id/log_image_preview"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <!-- Details Card -->
            <androidx.cardview.widget.CardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:cardCornerRadius="20dp"
                app:cardElevation="4dp"
                app:cardUseCompatPadding="true"
                app:cardBackgroundColor="@color/munch_white">

                <LinearLayout
                    android:orientation="vertical"
                    android:padding="20dp"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content">

                    <!-- Labels & Values -->
                    <TextView android:text="Food Name" style="@style/DetailLabel" />
                    <TextView android:id="@+id/value_food_name" style="@style/DetailValue" tools:text="Chicken" />

                    <TextView android:text="Meal" style="@style/DetailLabel" />
                    <TextView android:id="@+id/value_meal_type" style="@style/DetailValue" tools:text="Dinner" />

                    <TextView android:text="Logged At" style="@style/DetailLabel" />
                    <TextView android:id="@+id/value_logged_at" style="@style/DetailValue" tools:text="11/10/2025 at 18:30" />

                    <TextView android:text="Weight" style="@style/DetailLabel" />
                    <TextView android:id="@+id/value_weight" style="@style/DetailValue" tools:text="150 g" />

                    <TextView android:text="Calories (kcal)" style="@style/DetailLabel" />
                    <TextView android:id="@+id/value_calories" style="@style/DetailValue" tools:text="231" />

                    <TextView android:text="Protein (g)" style="@style/DetailLabel" />
                    <TextView android:id="@+id/value_protein" style="@style/DetailValue" tools:text="35" />

                    <TextView android:text="Carbohydrates (g)" style="@style/DetailLabel" />
                    <TextView android:id="@+id/value_carbs" style="@style/DetailValue" tools:text="0" />

                    <TextView android:text="Fat (g)" style="@style/DetailLabel" />
                    <TextView android:id="@+id/value_fat" style="@style/DetailValue" tools:text="9" />

                </LinearLayout>

            </androidx.cardview.widget.CardView>

        </LinearLayout>

    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>

